func make_adder(x: number): func(number): number {
    func adder(y: number): number {
        return x + y;
    }
    return adder;
}

let adder = make_adder(10);
let i = 0;
let total = 0;
while i < 1000000 {
    total = total + adder(i);
    i = i + 1;
}
print(total);
