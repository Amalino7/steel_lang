let my_list = [10,3,2,4,1,11,45, 23];

func cmp(a: number, b: number): boolean {
    return a < b;
}

let sorted = my_list.sort_by(less_than: cmp);
println(sorted);
println(sorted.reverse());
println(sorted.reverse().first(), " ", sorted.last());
println(my_list.contains(11));